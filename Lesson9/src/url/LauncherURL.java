package url;

public class LauncherURL {
    public static void main(String[] args) {
        GetURL getURL = new GetURL();
        String myURL = "http://sigma-site.com";

        getURL.getDataFromURL(myURL);

    }
}
